
{{#each searches}}
  <div class="saved-search">
    <form class="delete-saved-search" action="/savedSearches" method="post">
      <a href="/savedResults/{{this}}">#{{this}}</a>
      <button type="submit" name="delete" value="{{this}}">x</button>
    </form>
  </div>
{{/each}}
